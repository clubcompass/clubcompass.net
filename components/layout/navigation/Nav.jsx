import { NavWrapper } from "..";

export const Nav = () => {
  const name = "Abhinav Palacharla";
  const profileColor = "#F8DBFF";

  return <NavWrapper name={name} profileColor={profileColor} />;
};
